{
  "name": "igrs-hierarchy-migration",
  "version": "1.0.0",
  "type": "module",
  "description": "Database hierarchy migration scripts",
  "scripts": {
    "migrate": "node run-hierarchy-migration.js",
    "rollback": "node run-rollback.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.3.1",
    "pg": "^8.19.0"
  }
}
